import React from 'react';

const Balance = () => {
  const totalBalance = 5000; // Example balance amount

  return (
    <div>
      <h1>Balance</h1>
      <p>Total Balance: ${totalBalance}</p>
    </div>
  );
};

export default Balance;
